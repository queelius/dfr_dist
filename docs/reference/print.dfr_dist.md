# Print method for `dfr_dist` objects.

Print method for `dfr_dist` objects.

## Usage

``` r
# S3 method for class 'dfr_dist'
print(x, ...)
```

## Arguments

- x:

  The `dfr_dist` object to print.

- ...:

  Additional arguments (not used)
